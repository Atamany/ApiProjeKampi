@using ApiProjeKampi_WebUI.DTOs.TestimonialDTOs
@model List<ResultTestimonialDTO>
@{
    ViewData["Title"] = "TestimonialList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var count = 0;
}

<div class="section-body">
    <div class="row">
        <div class="col-12 col-md-12 col-lg-12">
            <div class="card">
                <div class="card-header">
                    <h4>Referanslar</h4>
                </div>
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Referans</th>
                                <th scope="col">Ünvan</th>
                                <th scope="col">Yorum</th>
                                <th scope="col">Sil</th>
                                <th scope="col">Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">
                                        @{
                                            count++;
                                        }
                                        @count
                                    </th>
                                    <td>@item.NameSurname</td>
                                    <td>@item.Title</td>
                                    <td>@item.Comment</td>
                                    <td><a href="~/Testimonial/DeleteTestimonial/@item.TestimonialId/" class="btn btn-danger">Sil</a></td>
                                    <td><a href="~/Testimonial/UpdateTestimonial/@item.TestimonialId/" class="btn btn-warning">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="~/Testimonial/CreateTestimonial/" class="btn btn-primary">Ekle</a>
                </div>
            </div>
        </div>
    </div>
</div>

